<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextView">
        <item name="android:textSize">@dimen/clinet_text_size</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

    <style name="EditTextValues">
        <item name="android:textSize">@dimen/clinet_hint_text_size</item>
        <item name="android:textColorHint">@color/textColor</item>
        <item name="android:textDirection">locale</item>
        <item name="android:layout_gravity">fill_horizontal</item>
        <item name="android:textColor">@color/editTextEntered</item>

    </style>

    <style name="Layout_basic">
        <item name="android:background">@drawable/form_background</item>
        <item name="android:layoutDirection">locale</item>
    </style>

    <style name="theme" parent="AppTheme" >
        <item name="android:colorBackground">@drawable/form_background</item>
    </style>
</resources>
